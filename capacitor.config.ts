import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.navalhaestilo.app',
  appName: 'Navalha & Estilo',
  webDir: 'dist',
  bundledWebRuntime: false
};

export default config;
